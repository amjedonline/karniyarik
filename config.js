var config = {};

config.development = {
  db: {
    name: "karniyarik"
  }
}

config.test = {
  db: {
    name: "test"
  }
}

config.live = {
  db: {
    name: "karniyarik"
  }
}

exports.config = config;
